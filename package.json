{
	"name": "folding-paper",
	"version": "0.1.0",
	"description": "Lets you fold paper and measure its surface area, volume, etc.",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"start": "node --env-file=.env app.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "MIT",
	"dependencies": {
		"@slack/bolt": "^4.2.0"
	}
}